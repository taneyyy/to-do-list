<% if not item.done %>
<tr>
   <td><%= link_to item.name,edit_item_path(item) %></td>
   <td><%= item.date %></td>
   <td>
      <%= link_to 'Mark as Done', mark_item_path(item), remote: true, class: "badge badge-primary"%>
   </td>
</tr>
<% end %>